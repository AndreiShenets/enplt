{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "ConnectionStrings": {
        "PostgresConnectionComment": "That is completely not fine to put real or dev connection strings into appsettings.json, but for challenge I think it is acceptable",
        "Postgres": "Host=localhost;Port=5432;Database=coding-challenge;Username=postgres;Password=mypassword123!"
    },
    "AllowedHosts": "*"
}
